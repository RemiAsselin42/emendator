# Backend (FastAPI) — copy to backend/.env and adjust. All vars are prefixed
# EMENDATOR_ (see app/config.py). Keep HOST on loopback.

EMENDATOR_PROFILE=1.21.1
EMENDATOR_HOST=127.0.0.1
EMENDATOR_PORT=8008
