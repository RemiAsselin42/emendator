# ---- OS / Editor ----
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# ---- Node / Vite / React (frontend) ----
node_modules/
dist/
dist-ssr/
*.local
.npm/
.pnp.*
.yarn/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- Tauri / Rust (desktop shell) ----
# Compiled artifacts; keep Cargo.toml + Cargo.lock under version control.
src-tauri/target/
target/
**/*.rs.bk
*.pdb

# ---- Python / FastAPI (backend) ----
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.Python
*.egg-info/
.eggs/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# ---- Environment / secrets ----
.env
.env.*
!.env.example

# ---- Runner runtime artifacts (Docker boots, logs, fixtures output) ----
# Captured boot logs / crash reports / mixin exports from headless server runs.
runtime-out/
*.log
crash-reports/
mixin-export/

# ---- Tooling / local meta (per global config; not part of the project) ----
graphify-out/
.mempalace/
