# Frontend (Vite) — copy to .env and adjust. Only VITE_*-prefixed vars are
# exposed to the browser bundle. Backend vars live in backend/.env.example.

# Base URL of the local FastAPI sidecar.
VITE_BACKEND_URL=http://127.0.0.1:8008
